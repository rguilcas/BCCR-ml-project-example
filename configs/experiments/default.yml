model:
  model_name: TwoLayerMLP
  input_channels: 1
  hidden_size: 32

data:
  data_in_name: msl_data.nc
  data_out_name: pr_era5_daily_westnorway_1940-2024.nc
  batch_size: 128

trainer:
  max_epochs: 20
  learning_rate: 0.0001
  accelerator: gpu
  devices: 1

logging:
  project: bccr-ml-course
  run_name: baseline
  base_dir: outputs